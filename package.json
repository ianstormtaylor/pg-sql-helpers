{
  "name": "pg-sql",
  "description": "A simple, safe and composable way to write SQL queries in Javascript.",
  "version": "0.0.0",
  "license": "MIT",
  "repository": "git://github.com/ianstormtaylor/pg-sql.git",
  "main": "./lib/index.js",
  "scripts": {
    "build": "babel ./src --out-dir ./lib",
    "clean": "rm -rf ./lib ./node_modules",
    "prepublish": "yarn run build",
    "release": "np",
    "test": "yarn run build && mocha --compilers js:babel-core/register ./test/index.js",
    "watch": "babel ./lib --out-dir ./lib --watch"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0",
    "mocha": "^3.2.0",
    "np": "^2.13.1"
  },
  "keywords": [
    "dynamic",
    "escape",
    "interpolate",
    "orm",
    "pg",
    "pgsql",
    "postgres",
    "postgresql",
    "query",
    "safe",
    "sql",
    "string",
    "template"
  ],
  "dependencies": {
    "is": "^3.2.1",
    "is-plain-object": "^2.0.4",
    "pg-sql": "^1.1.0"
  }
}
